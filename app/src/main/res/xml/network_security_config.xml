<?xml version="1.0" encoding="utf-8"?>
<!--
  Configuración de red para desarrollo:
  - Permite tráfico en claro (HTTP) hacia hosts típicos de dev: emulador, localhost
    y rangos LAN privados.
  - Para teléfono físico el usuario configura `api.host=<IP_LAN>:5223` en
    local.properties; añadir aquí la IP exacta si Android la rechaza.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">10.0.3.2</domain>
    </domain-config>
</network-security-config>
